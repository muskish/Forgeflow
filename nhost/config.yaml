version: 2
subdomain: local
region: local
auth:
  redirection:
    allowedUrls:
      - http://localhost:3000
postgres:
  version: "14"
hasura:
  adminSecret: nhost-admin-secret
  jwtSecret:
    type: HS256
    key: nhost-jwt-secret-key-must-be-at-least-32-chars-long
functions:
  nodeVersion: "18"
